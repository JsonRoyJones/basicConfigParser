// require in fs to read config file

const configController = {};

configController.getConfigValue = (req, res, next) => {
  // write code here
  // use fs to read config values and return the corresponding value
};

module.exports = configController;
